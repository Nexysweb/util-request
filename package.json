{
  "name": "@nexys/digis-i18n",
  "version": "0.9.1",
  "main": "dist/index.js",
  "dependencies": {
    "@babel/runtime": "^7.5.5",
    "axios": "^0.19.0",
    "node-polyglot": "^2.3.1",
    "store": "^2.0.12"
  },
  "scripts": {
    "test": "jest ",
    "build": "rimraf dist/ && babel src/ --out-dir dist/; rm dist/*.test.js"
  },
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "@nexys/babelsetup": "^0.0.6"
  }
}
